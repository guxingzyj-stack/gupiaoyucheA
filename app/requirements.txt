# ── A股智能分析预测系统 依赖列表 ──────────────────────────
# 安装命令：pip install -r requirements.txt

# 核心
streamlit>=1.50.0
akshare>=1.12.0
pandas>=2.0.0
numpy>=1.24.0
xgboost>=2.0.0
scikit-learn>=1.5.0
plotly>=5.17.0
requests>=2.32.4
beautifulsoup4>=4.12.0
snownlp>=0.12.0
jieba>=0.42.0
lxml>=4.9.1
joblib>=1.3.0
rich>=13.6.0

# 网络抓取（akshare 内部使用，必须）
curl_cffi>=0.7.0

# 定时任务（在线学习 scheduler 必须）
schedule>=1.2.0

# 贝叶斯优化（XGBoost超参数优化）
scikit-optimize>=0.9.0

# 情感分析（轻量，快速模式必须）
torch>=2.8.0
transformers>=4.53.0

# ── 可选：完整模式（Python ≤ 3.12）─────────────────────────
# LSTM 长期预测需要 tensorflow
# tensorflow>=2.13.0
# Prophet 时序预测
# prophet>=1.1.5
