# 版本更新日志

## v2.3.2 价值体检完善版 - 2026-06-19

### 新增（价值体检）
- 新增独立的价值体检旁路：包含门槛判断、质地 5 维、估值 3 维、红旗/提示、冲突警示和组合结论；该结果不并入现有 `total_score`，也不直接产出买卖信号。
- 质地维度覆盖 ROE、经营现金流、护城河、增长和资产负债表；估值维度覆盖历史 PE/PB 分位、质价匹配和股息率。
- 增加分行业口径：银行使用 PB/ROE 与监管指标缺失提示，周期/重资产使用正常化 PE，避免高点低 PE 陷阱。
- 增加 R2 多年营收趋势判断，用多年营收 CAGR 区分结构性下滑和单年波动；R2 触发时估值档显示为“便宜存疑”。
- 增加行业持久化缓存和已核实兜底映射，降低行业接口抽风导致银行/周期股分支失效的概率。
- 增加股息率兜底取数，实时行情缺失时可从历史分红股息率补充。
- 通用股质价匹配改为 PEG/ROE 启发式：优先使用多年归母净利润 CAGR，缺少多年口径时回退最新利润增速，增长无效时使用 ROE 锚。

## v2.3.1 渲染提速版 - 2026-05-29

### 优化（K线/图表渲染提速，数据一根不减，仍为 2 年 ~500 根）
- 关闭 K 线底部 rangeslider（`visible=False`）——它会把整条 K 线序列再绘制一遍，是最大的渲染开销；保留 1月/3月/6月/1年/全部 快捷缩放按钮。这是本次最有效的提速点。
- 传输数据精度压缩：价格保留 2 位小数、指标 3 位、成交量取整，数据点数量不变，仅减小传给浏览器的 JSON 体积。
- 桌面版与移动版共用 `create_candlestick_chart`，两端同步生效。
- 实测说明：单图 JSON 仅约 266KB、服务端建图 0.36s，瓶颈在浏览器端绘制约 485 根蜡烛的 SVG（固定开销，在保 2 年 + 全部指标前提下已近物理下限）。曾尝试将折线改 `go.Scattergl`（WebGL），但 485 点数据量下 WebGL 上下文启动开销反而拖慢，已回退为 `go.Scatter`（SVG）。

## v2.3.0 稳定优化版 - 2026-05-29

### 修复（健壮性）
- 模型加载跳过 0 字节 / 损坏 pkl 文件并校验结构，避免加载崩溃。
- 修正 `_should_accept_new_model` 接受阈值（1.01 → 1.0），不再容忍新模型劣化。
- `_load_latest_model` 按 timestamp 排序，确保取到真正最新版本。
- `daily_update` 预缓存各模型 RMSE，避免重复加载。
- 数据缓存读取异常处理，损坏缓存自动删除（`_safe_read_pickle`）。
- `_normalize_symbol` 改用 `zfill(6)`，不再 `lstrip("0")` 误伤代码。
- 新闻去重改用归一化全标题，关键词情绪逐篇 ±0.10 裁剪后取均值。
- 自选股保存路径加固；多处静默异常改为带日志处理。

### 优化（多核 / 性能）
- 并行配置按 CPU 核数动态推导（股票并行、XGB、集成训练、TF 线程），避免超额订阅。
- `board`/`compare` 多股票并行；Ensemble 5 个子模型并发训练；数据获取 IO 并行。
- 修复主 Streamlit 进程线程限制过严导致出图卡顿的问题。
- 修复 `mobile.py` 中 `n_jobs=-1` 抢占全部核心。
- HS300 成分共享数据内存缓存；启动时自动清理孤儿 / 0 字节模型文件。

### 变更
- LSTM 序列化改为架构 JSON + 权重（不含优化器状态），体积 192MB → ~30MB。
- 评分目标价由激进 `max(upper)` 改为保守 `mean + 0.5×half_width`。
- K 线图默认显示约 2 年（max_bars 250 → 500）。
- 安装 TensorFlow CPU 2.21.0，LSTM 由 MLP 兜底切换为 Keras 后端。

## v2.2.4 安装包构建脚本版 - 2026-05-18

### 新增
- 新增 `installer/build_installer.ps1`，可自动查找 Inno Setup 编译器并生成正式安装包。
- 新增 `installer/PACKAGING_NOTES.md`，记录安装包包含内容、排除内容、用户数据目录和打包命令。

### 优化
- 安装脚本版本号同步到 `2.2.4`，后续生成的安装包文件名可直接区分版本。

## v2.2.3 安装包准备版 - 2026-05-18

### 新增
- 新增正式启动器 `scripts/launch_app.py`，支持自动检查端口、启动服务并打开浏览器。
- 新增 `启动光环智能.bat`，作为正式安装包和桌面快捷方式的启动入口。
- 新增 Inno Setup 安装脚本草案 `installer/guanghuan_stock.iss`。
- 新增 `data/watchlist.py`，供后台任务在非 Streamlit 环境读取自选股。

### 优化
- 用户数据目录支持通过 `GUANGHUAN_STOCK_DATA_DIR` 独立配置，安装版可把自选股、缓存、模型、历史报告放到 LocalAppData。
- 在线学习模型、性能日志、预测历史、评分历史、缓存、输出目录改为统一走用户数据目录。
- 每日后台学习脚本改为动态写入当前安装目录和用户数据目录，避免安装到不同路径后计划任务失效。

## v2.2.2 新闻切换红错修复版 - 2026-05-18

### 修复
- 修复切换不同股票报告、回看其它报告时，相关新闻区域可能触发 Streamlit 前端 `removeChild on Node` 红错的问题。

### 优化
- 相关新闻区域由逐条折叠面板改为稳定表格展示，降低动态组件卸载时的前端节点冲突风险。

## v2.2.1 历史预测回顾版 - 2026-05-18

### 新增
- 新增历史预测回顾功能，可把过往预测报告与最新价格或正式验证结果进行对比。
- 预测回顾表支持查看 10 天、30 天预测的当时价格、预测价、实际/最新价、误差和方向判断。
- 单次历史预测支持展开查看当时评分快照和模型分项预测。

### 优化
- 预测快照新增评分记录，后续回顾时可查看当时综合评分和分项评分。
- 历史预测在目标日期未到时显示阶段对比，目标日期到达后自动锁定正式验证结果。

## v2.2.0 在线学习稳定版 - 2026-05-18

### 新增
- 新增在线学习后台执行入口，可从页面直接启动一次后台自学习。
- 新增每日后台学习计划任务注册与注销入口。
- 新增后台学习状态展示，包含计划任务状态、最近学习时间、最近报告和完成进度。
- 新增模型明细视图，展示最近学习报告、更新模型、跳过模型、警告数量和模型控制状态。
- 新增旧模型清理功能，可在在线学习页面手动清理当前自选股的旧模型版本。

### 优化
- 在线学习后台任务默认串行执行，避免一次点击启动多个训练窗口。
- 模型版本默认保留策略调整为每种模型保留最近 1 个版本和 RMSE 最优 1 个版本，降低长期使用后的磁盘占用。
- 最近学习报告支持跨天读取，会优先展示指定股票的最近一份报告。
- 模型明细页改为摘要和表格展示，提升可读性。
- 调整在线学习页和下拉列表样式，修复暗色主题下文字看不清的问题。

### 修复
- 修复 Streamlit 前端 `removeChild on Node` 红错。
- 修复多个按钮缺少唯一 `key` 导致的 `StreamlitDuplicateElementId` 错误。
- 修复后台自学习脚本误导入 Streamlit 页面导致的 `missing ScriptRunContext` 警告。
- 修复 `No module named 'app.models'; 'app' is not a package` 导入错误。
- 修复未安装 `schedule` 时反复告警的问题，改为使用独立调度脚本方案。
- 修复主界面 `use_container_width` 弃用提示，改为新版 `width` 参数。

### 已知事项
- `app/mobile.py` 中仍有少量 `use_container_width` 旧参数，若启用移动版页面，后续需要单独清理。
